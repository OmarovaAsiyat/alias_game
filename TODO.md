# TODO

## 🔴 Баги — начать с этого
- [x] ~~**Бесконечная игра**~~ — решено: игру открывали через `file://`, из-за чего словари не грузились через fetch; плюс баг был как раз тем самым отсутствием реального script.js (см. фикс от 11.09.2026). Через `python3 -m http.server` игра нормально доходит до экрана победителя. Логика nextTurn/showEndScreen проверена — сама по себе была корректна.

## Рефакторинг: разбить script.js на модули
Сейчас вся логика игры лежит в одном большом файле `script.js` (741 строка).
В папке `scripts/` уже есть файлы-заготовки под будущее разделение
(state.js, audio.js, effects.js, dictionary.js, navigation.js, game.js,
results.js, pause.js, endgame.js) — но они пустые, index.html их не
использует, script.js подключён напрямую (см. фикс от 11.09.2026).

**Принцип деления** — не «по размеру», а по зоне ответственности: файл =
один смысловой блок, который меняется вместе.

Карта разбиения (расписана по функциям):
- [ ] `state.js` — все глобальные переменные состояния игры (selectedLevel,
      CATEGORIES, WORDS, teams, scores, currentTeamIdx, currentRound,
      wordPool, usedWords, turnWords, currentWordObj, timerVal, roundsVal,
      soundOn, isPaused и т.д.)
- [ ] `audio.js` — весь звук (tone, playStartJingle, playConfirmBlip,
      playCoinSound, playHitSound, playNeutralTick, playVictoryFanfare,
      ensureAudio)
- [ ] `effects.js` — визуальные эффекты, не завязанные на игровую логику
      (buildRays, burst, burstAt, buildConfetti). `burst`/`burstAt` всё ещё
      используются (кнопка "start the game", выбор уровня, пилюли тем,
      Add/Remove team, ← Back) — переносим функции как есть. Вызовы на
      кнопках-Next/Play/Ready/Play Again/Resume уже убраны (см. «Общий
      стиль» — выполнено). `flash` в модуль **не переносим** — функция
      удалена из script.js целиком, её больше нет.
- [ ] `dictionary.js` — загрузка и разбор словарей (parseDictionary,
      applyLoadedDictionary, buildLevelList, syncLevelColor, slugifyCat)
- [ ] `navigation.js` — переключение экранов и мастер настроек (showScreen,
      handleStart, handleNext, handleLevelNext, handleTopicsNext,
      buildTopicPills, toggleAllTopics, adjTimer, adjRounds, buildTeamsList,
      addTeam, removeTeam)
- [ ] `game.js` — игровой цикл (buildWordPool, getNextWord, startGame,
      goToTurnScreen, beginTurn, loadNextWord, setupTimerBorder,
      updateTimerUI, pressBtn, handleGot, handleSkip)
- [ ] `results.js` — экран результатов и переход между ходами (goToResults,
      renderRoundSummary, renderResultsList, toggleResult,
      handleResultsNext, nextTurn)
- [ ] `pause.js` — пауза и выход из игры (openPause, closePause,
      resetQuitArm, handleQuit)
- [ ] `endgame.js` — финальный экран (setupTrophyArt, showEndScreen,
      handlePlayAgain)

План: аккуратно перенести код из script.js в эти файлы по карте выше,
обновить теги `<script>` в index.html, проверить что игра работает так же,
как раньше.

Статус: не начато.

## Общий стиль и эффекты
- [x] ~~Убрать эффект burst на кнопках в стиле "Got it"~~ — сделано.
      Все кнопки класса `.primary-btn` (визуально идентичны `.btn-gotit` —
      тот же пиксельный шрифт, тот же 3D-эффект нажатия) больше не вызывают
      burst: Next на уровне/темах/настройках, Ready, Play, Next Team/See
      Final Scores, Play Again, Resume в паузе. Сам эффект нажатия
      (`.press`, анимация "вдавливания" кнопки) остался — убрали только
      лучики поверх него.
      Burst **оставлен** там, где кнопки другого стиля: кнопка "start the
      game" на главном экране (класс `.cta`, текстовая, не primary-btn),
      строки выбора уровня, пилюли выбора тем, кнопки Add/Remove team,
      ← Back, значок звука в паузе.
- [x] ~~Убрать flash~~ — сделано, функция `flash()` и оба вызова
      (correct/skip на игровом экране) удалены из script.js целиком.
- [x] ~~Иконка паузы — квадратный блок~~ — `.pause-icon-btn` (⏸ в шапке
      игрового экрана) теперь квадрат 40×40 с рамкой и фоном, как и
      квадраты в самом меню паузы, а не голый эмодзи с паддингом.
- [x] ~~Иконка звука в паузе~~ — убрана совсем (кнопка `soundSquare`,
      функции `togglePauseSound()`/`syncPauseIcons()`): звук больше не
      выключается через паузу, просто всегда включён.
- [x] ~~Крестики удаления команд~~ — переделаны: невидимый квадратный
      блок под размер самого ✕ (без рамки/фона), пиксельный шрифт
      ('Press Start 2P'), серый (`var(--muted)`) когда неактивен (команд
      всего 2), белый (`var(--text)`) когда рабочий — burst на клике
      остался (был уже в JS).
- [ ] Кнопка **Quit** — в том же стиле, что и кнопка **Skip**.

## Мастер настройки игры — новые шаги (на будущее)
- [ ] Шаг: выбор книги или импорт материала.
- [ ] Шаг: выбор уровня.
- [ ] Шаг: убрать/изменить/добавить слова — редактируемый сет (**только**
      если материал не импортирован).
- [ ] Шаг настроек игры: добавить выбор "жаба или утка" (пока просто
      оставить место под эту опцию, без реализации).
- [ ] Режим **без команд**: игра без подсчёта очков; на экране endgame без
      победителя — просто картинка "Well done!".

## Choose the material
- [ ] Пока единственный доступный вариант — учебник **English File**.

## Choose your topics — готово
- [x] ~~Убрать заголовок "Vocabulary bank"~~ — убрана вместе со старым
      единственным переключающимся текстом кнопки.
- [x] ~~Добавить сверху две кнопки: "Select all" и "Clear"~~ — теперь это
      две отдельные кнопки (`selectAllTopics()` / `clearAllTopics()`)
      вместо одной с меняющимся текстом.
- [x] ~~Баг с "Clear"~~ — исправлено: `clearAllTopics()` теперь реально
      снимает выбор со всех тем (`selectedTopics = []`), раньше вместо
      этого выбиралась ровно одна первая тема.
- [x] ~~Кнопка Next деактивирована без выбранных тем~~ — работает "само
      собой": в `handleTopicsNext()` уже была проверка `if
      (selectedTopics.length === 0) return` *до* анимации нажатия и
      перехода — просто раньше до 0 тем нельзя было дойти из-за бага
      выше. Кнопка визуально не меняется (не сереет), просто не реагирует.
- [x] ~~Убрать нижнюю подпись про словари~~ — убрана обычная подпись
      "Words are loaded from...", но **оставлено** предупреждение на
      случай пустого словаря (это полезная информация, не мусор).
- [x] ~~Формат "File 6B" → "6B | Name"~~ — добавлена `formatTopicLabel()`,
      проверена на реальных категориях из словарей (see, e.g. "Words From
      Other Languages — File 6B" → "6B | Words From Other Languages").
      Темы без суффикса "File X" показываются как есть, без изменений.

## Round settings / Teams
- [x] ~~Блок Skip penalty без инструкций~~ — заголовок "Penalty for skip"
      + пояснение убраны, теперь просто "Skip penalty" с переключателем,
      как и просили.
- [x] ~~Кнопка Add своим блоком под списком команд, удаление крестиком~~ —
      сделано: убрали строку "Number of teams" с общими Remove/Add,
      каждая команда в списке теперь имеет свой ✕ (недоступен, если команд
      уже минимум 2), Add — отдельная кнопка на всю ширину под списком.
- [x] ~~Убрать инструкции про min/max команд~~ — текст "Min 2 · Max 6
      teams" убран с экрана (ограничение 2–6 в коде осталось — просто
      теперь не показывается как отдельная инструкция).
- [ ] **Отложено — опция "No teams"**: не стала делать походя вместе с
      мелкими правками выше, потому что это отдельная фича, а не
      косметика — она меняет логику на экране Teams (пропустить экран
      целиком?), Results (без имён команд и очков, только список слов) и
      Endgame (без победителя, просто "Well done!"). Это то же самое, что
      уже отмечено в разделе "Мастер настройки игры" ниже. Стоит сделать
      отдельным заходом, не смешивая с остальным.
- [ ] **Разблокировано** — таймер в настройках в пиксельном стиле:
      пиксельный таймер на игровом экране уже сделан (см. "Game screen"
      ниже — `.game-timer`, шрифт 'Press Start 2P', цвет var(--wiz-color)),
      можно взять этот же стиль сюда отдельным заходом. Пока не трогала —
      это отдельный экран, не хотела смешивать с игровым в одном коммите.

## Экран хода команды ("Team X, ваш ход") — готово
- [x] ~~Оставить только название команды и количество раундов~~ — карточка
      теперь показывает только `Round X of Y` и имя команды.
- [x] ~~Оставить фразу "It's your turn"~~ — не трогали, осталась как была.
- [x] ~~Убрать инструкции и перечисление тем~~ — убраны блок со списком
      выбранных тем (`turnCatList`/`.turn-cats`) и текст-подсказка
      "Describe each word without saying it...". Кнопка Ready и переход
      в игру (`beginTurn()`) не менялись.

## Game screen (карточка слова) — готово
- [x] ~~Название темы на карточке — нейтральный стиль~~ — убрала
      заливку-плашку цветом темы (`.word-cat`), оставила маленькую
      пиксельную подпись приглушённого серого цвета (`var(--muted)`),
      без фона.
- [x] ~~Таймер — пиксельный~~ — SVG-рамка вокруг карточки убрана целиком
      (`.timer-border-svg/-track/-fill`, `setupTimerBorder()`); вместо
      неё в шапке экрана квадратный блок `.game-timer` с числом секунд,
      шрифт 'Press Start 2P', цвет `var(--wiz-color)` (меняется по
      уровню, как и остальная тема экрана).
- [x] ~~Последнее слово остаётся на экране по истечении времени~~ —
      по таймауту больше не вызывается автопереход на results: таймер
      просто останавливается и красится в красный (`--skip`), карточка
      остаётся как есть. Переход на results теперь происходит только по
      следующему нажатию Got it / Skip — то есть можно доиграть
      последнее слово без спешки.
- [x] ~~Убрать flash-вспышку~~ — уже была убрана раньше (см. «Общий
      стиль» выше), тут отдельно делать было нечего.

## Round results
- [ ] Убрать перегружающий текст-инструкцию (инструкции по игре в целом
      планируются в другом месте, здесь не нужны). Дизайн сейчас не
      гармонирует с общей темой игры и перегружен текстом, хотя логика
      работает хорошо.
- [ ] Вместо красного/зелёного цвета для угаданных/пропущенных слов —
      красить цветом темы уровня.

## Endgame
- [ ] См. баг выше: экран сейчас недостижим из-за бесконечной игры.
- [ ] Трофей — картинку загрузит автор в папку проекта (assets/) и
      подключит через `setupTrophyArt`.
